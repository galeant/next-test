const { useEffect } = require("react")
const { Pagination } = require("react-bootstrap")

const PaginationWidget = ({ lastPage, currentPage, perPage, total }) => {

    const arrayPage = (lastPage) => {

    }

    useEffect(() => {

    })

    return (
        <Pagination></Pagination>
    )
}